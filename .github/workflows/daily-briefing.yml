name: Briefing diario Pokémon GO

on:
  schedule:
    # 12:00 UTC = 08:00 hora de Venezuela (UTC-4, sin horario de verano)
    - cron: "0 12 * * *"
  # Permite lanzarlo manualmente desde la pestaña "Actions" para probar
  workflow_dispatch:

permissions:
  contents: write

jobs:
  briefing:
    runs-on: ubuntu-latest
    steps:
      - name: Clonar el repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Descargar datos (raids, research, eventos, noticias)
        run: python fetch_data.py

      - name: Generar el briefing con la API gratuita de Gemini y enviarlo
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python generate_briefing.py

      - name: Guardar los resultados en el repositorio
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/latest.json briefing.md docs/pogo-briefing/index.html
          git diff --quiet && git diff --staged --quiet || git commit -m "Briefing diario $(date -u +%Y-%m-%d)"
          git push

